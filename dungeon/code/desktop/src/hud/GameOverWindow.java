package hud;

import com.badlogic.gdx.graphics.Color;

public class GameOverWindow extends Window{

    /** Constructor. Creates the game over window */
    public GameOverWindow() {
        super(14, 10, new Color(0.6f, 0.6f, 0.6f, 1));
    }
}
